services:
  damastes:
    image: damastes:latest
    volumes:
      - type: bind
        source: $HOME
        target: /enjoy
      - type: bind
        source: /run/media
        target: /run/media
    working_dir: /enjoy
